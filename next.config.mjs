/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['medical-chatbot-main.vercel.app', 'worka-out.vercel.app', 'ats-scorer-ai.vercel.app'],
  },
};

export default nextConfig;
